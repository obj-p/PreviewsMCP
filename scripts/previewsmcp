#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

bazelisk build //previewsmcp/cli:previewsmcp >&2

exec bazel-bin/previewsmcp/cli/previewsmcp "$@"
