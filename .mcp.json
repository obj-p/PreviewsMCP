{
  "mcpServers": {
    "previewsmcp": {
      "command": ".build/debug/previewsmcp",
      "args": ["serve"],
      "env": {
        "PATH": "${HOME}/.local/share/mise/shims:${PATH}"
      }
    }
  }
}
